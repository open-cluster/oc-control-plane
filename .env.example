# Copy to .env and fill in. `.env` is gitignored; this file is not, and must never hold a secret.
#
# Nothing here carries a credential. Every secret in this product is a FILE PATH, because an
# environment value is readable from a process listing and appears in every diagnostic dump of
# the environment. `.env` holds the paths; the files hold the keys.

# ---------------------------------------------------------------------------------------------
# Listeners. HTTP surfaces share one address; Relay keeps its own gRPC address.
# ---------------------------------------------------------------------------------------------

# Health, readiness and metrics. Required.
OC_HTTP_ADDRESS=127.0.0.1:8080

# The Relay endpoint. Empty serves no relays. With relays, OC_RELAY_SPKI_PINS is required.
#OC_RELAY_ADDRESS=127.0.0.1:8443
#OC_RELAY_SPKI_PINS=

# The operator surface. Empty exposes it nowhere; when enabled, use OC_HTTP_ADDRESS.
#OC_OPERATOR_ADDRESS=127.0.0.1:8080
#OC_OPERATOR_TOKEN_FILE=.secrets/operator.token
#OC_OPERATOR_TOKEN_ORGANIZATION=org-local
#OC_OPERATOR_PUBLIC_URL=
#OC_OPERATOR_CONSOLE_URL=
#OC_OPERATOR_ALLOWED_ORIGINS=
#OC_SEALING_KEY_FILE=.secrets/sealing.key
#OC_SEALING_KEY_ID=default
# During rotation, list the former key IDs and mounted files as comma-separated id=file entries.
#OC_PREVIOUS_SEALING_KEY_FILES=old=.secrets/sealing-old.key

# Alert intake. Empty takes no alerts. The public URL is what an Integration's webhook
# address is built from — it is pasted into a customer's own alerting, so it must be the
# origin THAT system reaches, never this listener's own.
#OC_INTAKE_ADDRESS=127.0.0.1:8080
#OC_INTAKE_PUBLIC_URL=

# ---------------------------------------------------------------------------------------------
# Storage. The database DSN carries a password, so the environment names its file.
# ---------------------------------------------------------------------------------------------

OC_DATABASE_DSN_FILE=.secrets/database.dsn

# ---------------------------------------------------------------------------------------------
# Providers reached outbound. The GitHub App is the deployment's credential; the model
# deployment is what investigations reason with. Consent lists the providers investigation
# material may be sent to — nothing listed permits nothing.
# ---------------------------------------------------------------------------------------------

#OC_GITHUB_APP_ID=
#OC_GITHUB_APP_PRIVATE_KEY_FILE=.secrets/github-app.pem
#OC_GITHUB_API_URL=
#OC_SLACK_API_URL=

#OC_MODEL_PROVIDER=anthropic
#OC_MODEL_NAME=claude-sonnet-5
#OC_MODEL_KEY_FILE=.secrets/model.key
#OC_MODEL_EFFORT=high
#OC_MODEL_CONSENTED_PROVIDERS=anthropic
#OC_MODEL_BASE_URL=

# ---------------------------------------------------------------------------------------------
# Optional.
# ---------------------------------------------------------------------------------------------

#OC_SHUTDOWN_TIMEOUT=15s
#OC_SERVICE_NAME=oc-control-plane
#OC_OTLP_ENDPOINT=
#OC_MINIMUM_RELAY_VERSION=
#OC_INVENTORY_INTERVAL=5m

# Conversations: multi-turn follow-up questions. Off until turned on; single-shot
# investigations are unaffected either way.
#OC_CONVERSATIONS_ENABLED=false
#OC_ORG_MAX_CONCURRENT_INVESTIGATIONS=4
#OC_ORG_MAX_WAITING_INVESTIGATIONS=16
#OC_MODEL_CONTEXT_WINDOW=
#OC_CONTEXT_THRESHOLD_PERCENT=50
#OC_CHANGE_LEDGER_RETENTION_DAYS=90
